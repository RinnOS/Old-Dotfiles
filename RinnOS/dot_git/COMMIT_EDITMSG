New package, sddm.
TODO: remake the whole thing, so it's possible to start from a fresh
Arch installation.
# Please enter the commit message for your changes. Lines starting
# with '#' will be ignored, and an empty message aborts the commit.
#
# On branch main
# Your branch is up to date with 'origin/main'.
#
# Changes to be committed:
#	modified:   1-base.sh
#	modified:   packages.yaml
#
# ------------------------ >8 ------------------------
# Do not modify or remove the line above.
# Everything below it will be ignored.
diff --git a/1-base.sh b/1-base.sh
index f88577f..780b17d 100644
--- a/1-base.sh
+++ b/1-base.sh
@@ -45,6 +45,10 @@ EOF
     ((I++))
 done
 
+echo 
+echo "Enabling services..."
+sudo systemctl enable sddm
+
 # Tell the user that a reboot is needed then to continue afterwards
 echo "Reboot is required to finish installation."
 echo "Please reboot and continue to run these scripts again afterwards."
diff --git a/packages.yaml b/packages.yaml
index a5157a1..b3e42bc 100644
--- a/packages.yaml
+++ b/packages.yaml
@@ -36,6 +36,7 @@ base:
   - flatpak
 
   # --- Uncategorized | Related to Wine hell ---
+  - sddm
   - i3-gaps
   - lxappearance
   - qt5ct
